# Copy to .env.local and fill in values from Supabase → Settings → API

NEXT_PUBLIC_SUPABASE_URL=https://vbccfhawjfcoetfmjbfd.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_RYu56yRsNbG_5CuAO_WaXQ_7lzTe4OB

# Optional: canonical site URL for email redirects and invite links (defaults to window.location.origin in the browser)
# NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Dev: always show survey + allow client-side submit checks. Run supabase/dev_force_survey_open.sql so RLS allows saves.
# Remove for production season dates.
# NEXT_PUBLIC_FORCE_SURVEY_OPEN=true
